<%
  const base = typeof baseUrl !== 'undefined' ? baseUrl : '';
  const active = typeof publicSection !== 'undefined' ? publicSection : '';
%>
<nav class="section-nav" aria-label="Secciones públicas">
  <a href="<%= base %>/actividades" class="section-nav__link<%= active === 'activities' ? ' is-active' : '' %>">Actividades</a>
  <a href="<%= base %>/libreria" class="section-nav__link<%= active === 'songs' ? ' is-active' : '' %>">Librería</a>
</nav>
